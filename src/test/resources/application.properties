spring.h2.console.enabled=false
spring.jpa.show-sql=false

# This is default property file in test. If we use this annotation @TestPropertySource
# we can set different classpath and different property file instead of default.
